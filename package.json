{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "npm_config_registry=\"http://localhost:4872/\" npx lerna publish from-git"
  },
  "devDependencies": {
    "lerna": "^5.0.0"
  }
}
